[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ai-journal-analyzer"
version = "0.2.8"
description = "CLI tool to analyze Linux journal entries with AI"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "platformdirs>=4.0",
]

[project.scripts]
ai-journal-analyzer = "ai_journal_analyzer:main"

[tool.hatch.build.targets.wheel]
only-include = []

[tool.hatch.build.targets.wheel.force-include]
"src/ai_journal_analyzer" = "ai_journal_analyzer.py"

[tool.hatch.build.targets.sdist]
include = [
    "README.md",
    "LICENSE",
    "Makefile",
    "pyproject.toml",
    "ai-journal-analyzer.conf.example",
    "src/ai_journal_analyzer",
    "scripts/install-simple.sh",
]
